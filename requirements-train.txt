# Training dependencies for HF Jobs runs.
# Base image must provide torch + CUDA (e.g. pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime).
# This file only adds the HF + RL stack and the repo deps.

# Versions aligned to what Unsloth's transitive deps actually allow:
#   - unsloth_zoo 2026.4.9 requires:  trl>=0.18.2, !=0.19.0, <=0.24.0
#   - we keep transformers/peft loose so Unsloth's pyproject can pick a working pair
#   - huggingface_hub <2.0 keeps push_to_hub API stable
trl>=0.18.2,!=0.19.0,<=0.24.0
transformers>=4.46
peft>=0.13
accelerate>=0.34
bitsandbytes>=0.43,<0.50
huggingface_hub>=0.25,<2.0
sentencepiece
einops
datasets

# Unsloth — used ONLY for fast model load + LoRA wrap (2× speed, fused kernels).
# Pulled from git so it always pairs with the latest torch/CUDA matrix; the
# [colab-new] extra is the recommended install on fresh containers.
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Repo deps
pydantic>=2.5
numpy>=1.24
PyYAML>=6.0
networkx>=3.1
matplotlib>=3.7
openenv-core
